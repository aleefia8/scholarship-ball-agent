services:
  - name: scholarship-ball-agent
    type: web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web_main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    healthCheckPath: /health
